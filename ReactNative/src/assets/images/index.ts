import {
  ImageSource,
  RemoteImage,
} from './type';

export {
  ImageSource,
  RemoteImage,
} from './type';

export const splash_default: ImageSource = {
  imageSource: require('./source/splash_default.png'),
};

export const splash: ImageSource = {
  imageSource: require('./source/splash.png'),
};
